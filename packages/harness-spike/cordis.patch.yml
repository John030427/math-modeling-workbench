# Package-level patch: insert self only (like dsh-mathmodeling).
# Disable ui-layout via PROFILE cordis.patch.yml for Live Gate control.
- insert:
    - id: mathmodel-harness-layout
      name: '@math-modeling/harness-spike'
